# AH-schedule

> 待补充：一句话说明这个项目是什么。

## Agent skills

### Issue tracker

Issues 存在本仓库的 GitHub Issues 里（`dearKJ/AH-schedule`），通过 `gh` CLI 读写。见 `docs/agents/issue-tracker.md`。

### Triage labels

沿用五个标准 triage 角色，但标签字符串使用中文：`待分诊` / `待补充信息` / `可交给智能体` / `待人工处理` / `不予处理`。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文（single-context）：根目录一个 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
