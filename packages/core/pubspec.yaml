name: ah_schedule_core
description: AH-schedule 的领域层——纯 Dart，不依赖 Flutter。
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

# 刻意留空：领域层不依赖任何运行期包。
# 「不依赖 Flutter」这条边界靠三重检查强制，见 test/no_flutter_dependency_test.dart。
dependencies:

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
